
import React from "react";

const Footer = () => {
  return (
    <footer className="mt-16 mb-8 text-center text-sm text-muted-foreground">
      <div className="container">
        <p>
          FileFlux Transformer — Convert any file format to another with ease.
        </p>
        <p className="mt-1">
          Note: This is a demo application. In a real-world scenario, conversions would be processed on a server.
        </p>
      </div>
    </footer>
  );
};

export default Footer;
